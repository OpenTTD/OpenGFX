# TTDPatch bundle:
$(TTDP_BUNDLE): $(GRF_FILES) $(DOC_FILES) $(TTDP_DOC)
	$(_E) "[BUNDLE] for TTDPatch"
	$(_V) -rm -rf $@
	$(_V) mkdir $@
	$(_V) for i in $(GRF_FILES); do cp $$i $@/`echo $$i | sed -e 's/ogfx\([1|c|h|i|t]\)_[a-z]*/trg\1r/'` ; done
	$(_V) cp $(DOC_FILES) $@/
	$(_V) cp $(TTDP_DOC) $@/

$(TTDP_BUNDLE)-$(VERSION_STRING).zip: $(TTDP_BUNDLE)
	$(_E) "[ZIP] $<"
	$(_V) pushd $< && $(ZIP) $(ZIP_FLAGS) ../$@ * >/dev/null && popd

bundle_ttdp: $(TTDP_BUNDLE)-$(VERSION_STRING).zip


ifdef USE_NML
sprites/nfo/base/%.pnfo: sprites/nml/base/%.bnml
	$(_E) "[NML2NFO] $@"
	$(_V) $(CC) -D REPO_REVISION=$(REPO_REVISION) $(CC_FLAGS) $< | $(NML) $(NML_FLAGS) --nfo $@ --start-sprite=`echo "$<" | cut -d- -f2 | sed "s/^0*//"`
sprites/nfo/arctic/%.pnfo: sprites/nml/arctic/%.bnml
	$(_E) "[NML2NFO] $@"
	$(_V) $(CC) -D REPO_REVISION=$(REPO_REVISION) $(CC_FLAGS) $< | $(NML) $(NML_FLAGS) --nfo $@ --start-sprite=`echo "$<" | cut -d- -f2 | sed "s/^0*//"`
sprites/nfo/tropical/%.pnfo: sprites/nml/tropical/%.bnml
	$(_E) "[NML2NFO] $@"
	$(_V) $(CC) -D REPO_REVISION=$(REPO_REVISION) $(CC_FLAGS) $< | $(NML) $(NML_FLAGS) --nfo $@ --start-sprite=`echo "$<" | cut -d- -f2 | sed "s/^0*//"`
sprites/nfo/toyland/%.pnfo: sprites/nml/toyland/%.bnml
	$(_E) "[NML2NFO] $@"
	$(_V) $(CC) -D REPO_REVISION=$(REPO_REVISION) $(CC_FLAGS) $< | $(NML) $(NML_FLAGS) --nfo $@ --start-sprite=`echo "$<" | cut -d- -f2 | sed "s/^0*//"`
sprites/nfo/logos/%.pnfo: sprites/nml/logos/%.bnml
	$(_E) "[NML2NFO] $@"
	$(_V) $(CC) -D REPO_REVISION=$(REPO_REVISION) $(CC_FLAGS) $< | $(NML) $(NML_FLAGS) --nfo $@ --start-sprite=`echo "$<" | cut -d- -f2 | sed "s/^0*//"`
sprites/nfo/extra/%.pnfo: sprites/nml/extra/%.bnml
	$(_E) "[NML2NFO] $@"
	$(_V) $(CC) -D REPO_REVISION=$(REPO_REVISION) $(CC_FLAGS) $< | $(NML) $(NML_FLAGS) --nfo $@
endif
